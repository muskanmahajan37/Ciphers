message = "how hoe are you what are you doing"
transilator=""
i = len(message) - 1
while i > 0:
    transilator = transilator + message[i]
    # print(i,message[i],transilator)
    i = i -1
print(transilator)